[phases.setup]

[phases.install]
cmds = [
  "echo \"Authenticating with Docker Hub...\"",
  "echo $DOCKER_PASSWORD | docker login docker.io -u $DOCKER_USERNAME --password-stdin"
]

[start]
cmd = "docker run -p 3000:3000 -e WAHA_LICENSE_KEY=$WAHA_LICENSE_KEY devlikeapro/waha-plus:latest"
```

### **2. Mantenha o Dockerfile** (não precisa mexer nele)

### **3. No Railway - Adicione as variáveis:**
```
WAHA_LICENSE_KEY=d0e7_pci_tMFcC2gfn4cGrSPym6cyNkszYtLSD
PORT=3000
DOCKER_USERNAME=devlikeapro
DOCKER_PASSWORD=dckr_pat_tMFuCZgNdkqZp9peXcyN4a75L30
